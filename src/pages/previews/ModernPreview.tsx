import { weddingData } from "@/data/weddingData";
import { ModernFlipInvite } from "@/templates";

const ModernPreview = () => {
  return <ModernFlipInvite data={weddingData} />;
};

export default ModernPreview;
